# Cognito (backend/template.yaml の SAM デプロイ Outputs から取得)
# ブラウザ側でも使うため NEXT_PUBLIC_ プレフィックスが必要
NEXT_PUBLIC_COGNITO_USER_POOL_ID=us-east-1_xxxxxxxxx
NEXT_PUBLIC_COGNITO_USER_POOL_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx

# AgentCore Runtime (agentcore/.cli/deployed-state.json や `agentcore status` から取得)
# ブラウザから直接 AgentCore Runtime を呼び出すため NEXT_PUBLIC_ プレフィックスが必要。
# 呼び出しには Cognito が発行した JWT (Inbound Auth で aud クレームを検証) が必須なため、
# ARN 自体がブラウザに見えても実害は小さい。
NEXT_PUBLIC_AGENT_RUNTIME_ARN=arn:aws:bedrock-agentcore:us-east-1:xxxxxxxxxxxx:runtime/examAgent_MyAgent-xxxxxxxxxx
NEXT_PUBLIC_AGENT_RUNTIME_REGION=us-east-1
